#!/bin/bash

  bower install;
  npm install

  grunt;
  grunt test;

  grunt serve;

  echo "npm: "; npm -v; echo \n;
  echo "node: "; node -v; echo \n;
  echo "grunt: "; grunt -v; echo \n;
  echo "bower: "; bower -v; echo \n;
