node.js > express.js

Why use node.js? Web application servers (extremely fast)

//Install: (dont install with brew)
npm install -g express
npm install -g express-generator
npm install -g nodemon
  
  //Generate APP/folder:
    express testexpress
    cd testexpress && npm install
    DEBUG=testexpress ./bin/www
    //Open in browser:
      http://localhost:3000/

    // HAS
      index.jade
      public/stylesheets/style.css

  //Alternative version (hogan ~ mustache) Generate APP/folder:
    express expresshogan --hogan -c less
    cd expresshogan && npm install
    DEBUG=expresshogan ./bin/www
    //Open in browser:
      http://localhost:3000/
    //must also run this to get real-time: (it does node monitoring) 
    // doesn't work for some reason...
      nodemon app.js


    // HAS
      app.js
      index.hjs

      public/stylesheets/style.less  (less-middleware = real-time LESS)

      

















//http://expressjs.com/
//https://www.youtube.com/watch?v=ndKRjmA6WNA
// learning node.js livelessons